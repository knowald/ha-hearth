name: Bug report
description: Something does not work as expected
labels: [bug]
body:
  - type: input
    id: version
    attributes:
      label: Hearth version
      description: Shown in application settings, or the image tag for Docker installs.
      placeholder: 0.1.0
    validations:
      required: true

  - type: dropdown
    id: install-method
    attributes:
      label: Install method
      options:
        - Docker
        - Local development (Node.js)
    validations:
      required: true

  - type: input
    id: ha-version
    attributes:
      label: Home Assistant version
      placeholder: 2026.6.3
    validations:
      required: true

  - type: input
    id: browser
    attributes:
      label: Browser and device
      placeholder: Safari on iPad, Chrome on desktop, Fully Kiosk on tablet
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: What happened
      description: What did you do, what did you expect, what happened instead?
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: Logs and console output
      description: Server logs or `docker logs ha-hearth`, plus browser console errors (F12 > Console). Screenshots help too.
      render: text

  - type: textarea
    id: config
    attributes:
      label: Relevant hearth.yaml section
      description: The card, widget or page involved. Remove anything private.
      render: yaml
